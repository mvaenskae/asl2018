all:
  hosts:
    asl-cnc
  children:
    clients:
      hosts:
        asl-c[1:3]
    middlewares:
      hosts:
        asl-m[1:2]
    backends:
      hosts:
        asl-s[1:3]
  vars:
    ansible_connection: ssh
    ansible_user: asl
