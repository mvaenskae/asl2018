---
- name: Updating Middleware JAR
  hosts: middleware
  vars_files:
      - ../constants.yml

  tasks:
    - name: Pushing to server
      synchronize:
        src: ../../../../middleware/dist/middleware-mickeyv.jar
        dest: "{{ mw_jar_path }}"
